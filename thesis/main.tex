\documentclass[12pt, twoside]{report}
\usepackage[backend=biber, sorting=nty, style=ieee]{biblatex}
\usepackage{fancyhdr}
\usepackage[a4paper, width=150mm, top=25mm, bottom=25mm, left=35mm, right=35mm, bindingoffset=0mm]{geometry}
\usepackage{hyperref}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage[english, ngerman]{babel}
\usepackage{listings}
\usepackage{epstopdf}
\usepackage{forest}
\usepackage{subcaption}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{sourcecodepro}
\usepackage[T1]{fontenc}
\usepackage{amssymb}
\usepackage{tabularx}
\usepackage{emptypage}
\usepackage{pdflscape}
\usepackage{wasysym}
\usepackage{float}

\bibliography{bibliography}

\graphicspath{ {images/} }
\usetikzlibrary{matrix,arrows,positioning,shadows}
\tikzset{
	feature/.style={draw, inner sep=1.5mm, font=\small\sffamily, fill=white, drop shadow},
opt/.style={fill=white}
}

\pagestyle{fancy}
\headheight = 16pt
\fancyhead[OL,ER]{}
\fancyhead[OC,EC]{}
\fancyhead[OR,EL]{\leftmark}
\fancyfoot[OL,ER]{}
\fancyfoot[OC,EC]{}
\fancyfoot[OL,ER]{\thepage}
\fancypagestyle{plain}{
	\fancyhf{}
	\fancyfoot[OL,ER]{\thepage}
	\renewcommand{\headrulewidth}{0pt}
}

\lstset{
	aboveskip=2em,
	basicstyle=\footnotesize\ttfamily,
	breakatwhitespace=true,
	breaklines=true,
	captionpos=b,
	frame=single,
	keepspaces=true,
	numbers=left,
	postbreak=\raisebox{0ex}[0ex][0ex]{\ensuremath{\color{red}\hookrightarrow\space}},
	showstringspaces=false,
	showtabs=false,
	tabsize=2,
	title=\lstname,
	xleftmargin=2em,
	literate={λ}{{$\lambda$}}1
}

\lstdefinelanguage{rascal}{
	alsoletter=-,
	morekeywords={
		bool,
		bottom-up-break,
		case,
		class,
		default,
		do,
		extend,
		import,
		final,
		lexical,
		list,
		loc,
		module,
		node,
		package,
		private,
		public,
		rel,
		return,
		set,
		solve,
		start,
		static,
		str,
		syntax,
		switch,
		test,
		visit,
		while,
		o
	},
	morestring=[b]"
}

\lstdefinelanguage{spoofax}{
	alsoletter=-,
	morekeywords={
		binding,
		context-free,
		constructors,
		defines,
		error,
		id,
		imports,
		LAYOUT,
		lexical,
		module,
		namespaces,
		refers,
		restrictions,
		rules,
		scopes,
		signature,
		start,
		start-symbols,
		symbol,
		syntax,
		test,
		to,
		unique,
		warning,
		where,
		with
	}
}

\lstdefinelanguage{racket}{
	alsoletter=/->\#,
	morekeywords=[1]{
		datum->syntax,
		define,
		define-struct,
		define-syntax,
		lambda,
		provide,
		require,
		string->symbol,
		syntax-case,
		syntax->datum,
		with-syntax
	},
	morekeywords=[2]{
		\#lang
	},
	morestring=[b]"
}

\begin{document}
\include{cover}

\cleardoublepage
\include{declaration}

\cleardoublepage
\include{abstract}

\include{acknowledgement}

\cleardoublepage
\pagenumbering{roman}\setcounter{page}{1}
\tableofcontents
\listoffigures
\lstlistoflistings

\cleardoublepage
\pagenumbering{arabic}\setcounter{page}{1}
\include{chapters/introduction}
\cleardoublepage
\include{chapters/related_work}
\cleardoublepage
\include{chapters/implementation}
\cleardoublepage
\include{chapters/features}
\cleardoublepage
\include{chapters/conclusion}
\cleardoublepage
\include{chapters/bibliography}
\end{document}
